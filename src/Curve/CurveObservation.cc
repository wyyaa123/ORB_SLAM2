#include "Curve/CurveObservation.h"

namespace ORB_SLAM2
{
    CurveSampleCorrespondence::CurveSampleCorrespondence() : pMapCurve(NULL), observedCurveIndex(0), observedParameter(0.0), initialDistance(0.0), normalizedWeight(0.0)
    {
    }
}
